<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Guías para agentes — Nu</title>
  <style>
    :root {
      --bg: #0f0f12;
      --surface: #1a1a21;
      --border: #2e2e3a;
      --text: #f4f4f6;
      --muted: #9b9ba8;
      --accent: #7c3aed;
      --accent-soft: rgba(124, 58, 237, 0.2);
      --step-num: #a78bfa;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: "Segoe UI", system-ui, -apple-system, sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.55;
      min-height: 100vh;
    }

    header {
      background: var(--surface);
      border-bottom: 1px solid var(--border);
      padding: 1rem 1.25rem;
      position: sticky;
      top: 0;
      z-index: 10;
    }

    .header-inner {
      max-width: 1100px;
      margin: 0 auto;
    }

    h1 {
      font-size: 1.1rem;
      font-weight: 600;
      margin: 0 0 0.75rem 0;
      color: var(--muted);
      letter-spacing: 0.02em;
    }

    .topics {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
    }

    .topic-btn {
      appearance: none;
      border: 1px solid var(--border);
      background: var(--bg);
      color: var(--text);
      padding: 0.55rem 1rem;
      border-radius: 999px;
      font-size: 0.9rem;
      cursor: pointer;
      transition: border-color 0.15s, background 0.15s, color 0.15s;
    }

    .topic-btn:hover:not(:disabled) {
      border-color: var(--accent);
      color: var(--step-num);
    }

    .topic-btn[aria-pressed="true"] {
      background: var(--accent-soft);
      border-color: var(--accent);
      color: #e9d5ff;
    }

    .topic-btn:disabled {
      opacity: 0.45;
      cursor: not-allowed;
    }

    main {
      max-width: 1100px;
      margin: 0 auto;
      padding: 1.5rem 1.25rem 3rem;
    }

    .panel {
      display: none;
      animation: fadeIn 0.25s ease;
    }

    .panel.active {
      display: block;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(6px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .panel h2 {
      font-size: 1.35rem;
      margin: 0 0 0.35rem 0;
      font-weight: 700;
    }

    .panel .subtitle {
      color: var(--muted);
      font-size: 0.95rem;
      margin: 0 0 1.25rem 0;
    }

    .step-layout {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1.25rem;
    }

    @media (min-width: 860px) {
      .step-layout {
        grid-template-columns: minmax(280px, 380px) 1fr;
        align-items: start;
      }
    }

    .step-list-wrap {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 0.65rem;
    }

    .step-list-wrap h3 {
      margin: 0 0 0.5rem 0.35rem;
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: var(--muted);
    }

    ol.guide-steps {
      margin: 0;
      padding: 0;
      list-style: none;
    }

    .guide-step-btn {
      width: 100%;
      text-align: left;
      display: flex;
      gap: 0.65rem;
      align-items: flex-start;
      padding: 0.7rem 0.65rem;
      margin-bottom: 0.35rem;
      border: 1px solid transparent;
      border-radius: 10px;
      background: transparent;
      color: var(--text);
      font: inherit;
      line-height: 1.45;
      cursor: pointer;
      transition: background 0.15s, border-color 0.15s;
    }

    .guide-step-btn:last-child {
      margin-bottom: 0;
    }

    .guide-step-btn:hover {
      background: rgba(255, 255, 255, 0.04);
    }

    .guide-step-btn.is-active {
      background: var(--accent-soft);
      border-color: var(--accent);
    }

    .guide-step-num {
      flex-shrink: 0;
      width: 1.65rem;
      height: 1.65rem;
      border-radius: 8px;
      background: var(--accent-soft);
      color: var(--step-num);
      font-size: 0.8rem;
      font-weight: 700;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .guide-step-btn.is-active .guide-step-num {
      background: var(--accent);
      color: #fff;
    }

    .step-visual {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 1rem;
      min-height: 200px;
      position: sticky;
      top: 5.5rem;
    }

    .step-visual h3 {
      margin: 0 0 0.75rem 0;
      font-size: 0.95rem;
      font-weight: 600;
      color: var(--muted);
    }

    .step-visual img {
      display: block;
      width: 100%;
      max-width: 420px;
      margin: 0 auto;
      border-radius: 12px;
      border: 1px solid var(--border);
      background: #000;
    }

    .step-visual img.is-hidden {
      display: none;
    }

    .img-missing {
      display: none;
      text-align: center;
      padding: 2rem 1rem;
      color: var(--muted);
      font-size: 0.9rem;
      border: 1px dashed var(--border);
      border-radius: 12px;
      max-width: 420px;
      margin: 0 auto;
    }

    .img-missing.is-visible {
      display: block;
    }

    .img-missing code {
      font-size: 0.8rem;
      color: var(--step-num);
      word-break: break-all;
    }

    .placeholder-msg {
      color: var(--muted);
      padding: 2rem;
      text-align: center;
      background: var(--surface);
      border: 1px dashed var(--border);
      border-radius: 12px;
    }
  </style>
</head>
<body>
  <header>
    <div class="header-inner">
      <h1>Guías paso a paso — soporte al cliente</h1>
      <nav class="topics" aria-label="Temas de guía">
        <button type="button" class="topic-btn" data-panel="cert-ret" aria-pressed="true">
          Certificados de RET
        </button>
        <button type="button" class="topic-btn" data-panel="limite" aria-pressed="false" disabled title="Próximamente">
          Límite
        </button>
        <button type="button" class="topic-btn" data-panel="mas" aria-pressed="false" disabled title="Próximamente">
          Más temas…
        </button>
      </nav>
    </div>
  </header>

  <main>
    <section id="panel-cert-ret" class="panel active" aria-labelledby="titulo-cert-ret">
      <h2 id="titulo-cert-ret">Certificados de RET</h2>
      <p class="subtitle">Toca un paso para ver la captura que debería mostrar la app del cliente. Guía al cliente en el orden de la lista.</p>

      <div class="step-layout">
        <div class="step-list-wrap">
          <h3>Pasos</h3>
          <ol class="guide-steps" id="ret-step-list">
            <li>
              <button type="button" class="guide-step-btn is-active" data-img="imagenes/01-app-inicio.png" aria-current="step">
                <span class="guide-step-num">1</span>
                <span>Abre la app de Nu.</span>
              </button>
            </li>
            <li>
              <button type="button" class="guide-step-btn" data-img="imagenes/01-app-inicio.png">
                <span class="guide-step-num">2</span>
                <span>En la parte superior izquierda, toca el <strong>icono morado del perfil</strong>.</span>
              </button>
            </li>
            <li>
              <button type="button" class="guide-step-btn" data-img="imagenes/03-menu-documentos.png">
                <span class="guide-step-num">3</span>
                <span>Selecciona <strong>«Documentos»</strong>.</span>
              </button>
            </li>
            <li>
              <button type="button" class="guide-step-btn" data-img="imagenes/04-generar-documentos-ret.png">
                <span class="guide-step-num">4</span>
                <span>En <strong>«Generar documentos»</strong>, elige <strong>«Certificado anual de retenciones en la fuente»</strong>.</span>
              </button>
            </li>
            <li>
              <button type="button" class="guide-step-btn" data-img="imagenes/05-elegir-anho.png">
                <span class="guide-step-num">5</span>
                <span>Selecciona el <strong>año</strong> que desea consultar.</span>
              </button>
            </li>
            <li>
              <button type="button" class="guide-step-btn" data-img="imagenes/06-enviar-reporte.png">
                <span class="guide-step-num">6</span>
                <span>Toca <strong>«Enviar reporte»</strong> o <strong>«Enviar reporte/certificado»</strong> (según lo que muestre la app).</span>
              </button>
            </li>
            <li>
              <button type="button" class="guide-step-btn" data-img="imagenes/07-correo-listo.png">
                <span class="guide-step-num">7</span>
                <span>Pide que revise su <strong>correo</strong> y que abra el documento (si la app lo indica, con su <strong>número de identificación</strong>).</span>
              </button>
            </li>
          </ol>
        </div>

        <div class="step-visual" aria-live="polite">
          <h3 id="ret-preview-label">Vista del cliente</h3>
          <img
            id="ret-step-img"
            src="imagenes/01-app-inicio.png"
            alt="Captura de pantalla del paso seleccionado en la app Nu."
          />
          <p id="ret-img-missing" class="img-missing" hidden>
            No se encontró la imagen. Guarda tu captura en la carpeta <code>imagenes/</code> con los nombres usados en cada paso (por ejemplo <code>01-app-inicio.png</code>).
          </p>
        </div>
      </div>
    </section>

    <section id="panel-limite" class="panel" aria-labelledby="titulo-limite" hidden>
      <h2 id="titulo-limite">Límite</h2>
      <p class="placeholder-msg">Aquí irá la guía cuando la tengas lista.</p>
    </section>

    <section id="panel-mas" class="panel" aria-labelledby="titulo-mas" hidden>
      <h2 id="titulo-mas">Más temas</h2>
      <p class="placeholder-msg">Agrega nuevos botones en el encabezado y paneles con los pasos.</p>
    </section>
  </main>

  <script>
    (function () {
      var topicButtons = document.querySelectorAll(".topic-btn[data-panel]");

      function showPanel(id) {
        document.querySelectorAll(".panel").forEach(function (el) {
          var suffix = el.id.indexOf("panel-") === 0 ? el.id.slice(6) : el.id;
          var on = suffix === id;
          el.classList.toggle("active", on);
          el.hidden = !on;
        });
        topicButtons.forEach(function (btn) {
          var pressed = btn.getAttribute("data-panel") === id;
          btn.setAttribute("aria-pressed", pressed ? "true" : "false");
        });
      }

      topicButtons.forEach(function (btn) {
        if (btn.disabled) return;
        btn.addEventListener("click", function () {
          var id = btn.getAttribute("data-panel");
          if (id) showPanel(id);
        });
      });

      var stepList = document.getElementById("ret-step-list");
      var previewImg = document.getElementById("ret-step-img");
      var missingEl = document.getElementById("ret-img-missing");

      function setActiveStep(btn) {
        if (!stepList || !btn) return;
        stepList.querySelectorAll(".guide-step-btn").forEach(function (b) {
          b.classList.toggle("is-active", b === btn);
          if (b === btn) b.setAttribute("aria-current", "step");
          else b.removeAttribute("aria-current");
        });
        var src = btn.getAttribute("data-img");
        if (previewImg && src) {
          previewImg.classList.remove("is-hidden");
          if (missingEl) {
            missingEl.classList.remove("is-visible");
            missingEl.hidden = true;
          }
          var numEl = btn.querySelector(".guide-step-num");
          previewImg.alt =
            "Referencia visual en la app Nu para el paso " + (numEl ? numEl.textContent : "") + ".";
          previewImg.src = src;
        }
      }

      function showMissingPlaceholder() {
        if (!previewImg || !missingEl) return;
        previewImg.classList.add("is-hidden");
        missingEl.hidden = false;
        missingEl.classList.add("is-visible");
      }

      if (previewImg) {
        previewImg.addEventListener("error", showMissingPlaceholder);
        previewImg.addEventListener("load", function () {
          if (previewImg.naturalWidth > 0) {
            previewImg.classList.remove("is-hidden");
            if (missingEl) {
              missingEl.classList.remove("is-visible");
              missingEl.hidden = true;
            }
          }
        });
      }

      if (stepList) {
        stepList.addEventListener("click", function (e) {
          var btn = e.target.closest(".guide-step-btn");
          if (!btn || !stepList.contains(btn)) return;
          setActiveStep(btn);
        });

        var first = stepList.querySelector(".guide-step-btn.is-active") || stepList.querySelector(".guide-step-btn");
        if (first) {
          var initial = first.getAttribute("data-img");
          if (previewImg && initial) {
            previewImg.src = initial;
          }
          if (previewImg && previewImg.complete && previewImg.naturalWidth === 0) {
            showMissingPlaceholder();
          }
        }
      }
    })();
  </script>
</body>
</html>
